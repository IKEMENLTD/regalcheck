[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# 環境変数（Netlify管理画面で設定してください）
# ANTHROPIC_API_KEY = "your_api_key_here"
